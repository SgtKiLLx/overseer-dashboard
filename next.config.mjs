/** @type {import('next').NextConfig} */
const nextConfig = {
    // This allows images from Discord to show up (like your profile pic)
    images: {
        domains: ['cdn.discordapp.com'],
    },
};

export default nextConfig;
